import 'package:flutter/material.dart';

class ColorData {
  final String name;
  final Color color;
  final String description;

  const ColorData(this.name, this.color, this.description);
}
